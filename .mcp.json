{
  "_comment": "IGNIS MCP server. Uses the global `ignis-mcp` bin from @vaultspark/ignis-core (install once per machine: `npm i -g <path-to-vaultspark-ignis-checkout>`). Portable — no absolute local paths (public-repo sanitization).",
  "mcpServers": {
    "ignis": {
      "command": "npx",
      "args": [
        "ignis-mcp"
      ],
      "env": {
        "IGNIS_PROJECT_ROOT": "."
      }
    }
  }
}
