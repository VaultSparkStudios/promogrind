# Current State — PromoGrind

Last updated: 2026-07-25 (Session 119)

PromoGrind remains deployed/public-unlaunched in FORGE launch-hardening. S119 completed the continuous `/arc`, exhausted four premise-verified infrastructure items, then shipped four second-order truth, deployment, and closeout ratchets.

Launch proofs now require criterion-level typed receipt quorums; neither a generic note nor a stale persisted status can claim completion. Startup signals declare unavailable/empty/failing states honestly, full-history secret archaeology finishes in about two seconds, and five launch capabilities report target identity and scope instead of credential presence alone. The Pages build now injects browser-safe capture configuration into every capture surface and fails closed in Continuous Integration (CI) when that production key is absent.

Repo-owned verification is green: 590/590 Vitest checks across 75 files, 47 new standalone invariant assertions, source/claims/entitlement/auth/launch/UX/browser integrity gates, bundle budgets, proof replay, sanitization, and `npm run verify:launch-local` with direct exit 0.

Release truth remains NO-GO for a SPARKED promotion. Brevo sender-domain authentication is now READY and the exact Supabase project plus live Stripe account are authorized; the live origin still lacks six required headers, the exact Cloudflare contact-forwarding rule is unreadable with current token scope, capture configuration awaits this commit's deployment, and human email/purchase/friend proofs remain pending. The historical scanner isolated one real privileged Supabase JavaScript Object Signing and Encryption (JOSE) token for rotation; redacted remediation cargo `01JUE23NQ1EEF6010874B09F97` is with Studio Ops.
