<!-- truth-audit-version: 1.1 -->
# Truth Audit

Overall status: green
Last reviewed: 2026-04-17 (S65 Studio OS/ops conformance — 289/289 tests, build green, ops registry complete, bundle 329.3KB/425KB)
Key source-of-truth changes this session:
- `docs/SESSION_PROTOCOL.md` now matches the canonical Studio OS initiation check (`^## [0-9]` for SIL entries).
- `scripts/ops.mjs` plus the registered Studio Ops command targets are now present locally; `/start`, `/go`, and `/closeout` no longer need the missing-script public-repo fallback for core automation.
- `scripts/detect-session-mode.mjs` keeps PromoGrind in BUILDER mode unless current-session intent explicitly indicates Founder mode.
- `docs/STARTUP_BRIEF.md` is generated by the imported startup brief renderer and passes the canonical brief validator.
- `supabase/functions/_shared/validate.ts` is now the single source of truth for VALID_CALCULATOR_SLUGS and VALID_PROMO_TYPES (previously each edge function had its own inline arrays)
- `src/lib/featureFlags.js` is now the client source of truth for remote feature flag resolution; `FEATURE_FLAGS` in launchState.js remains the build-time fallback; S64 gates now cover PromoAdvisor, PromoChat, AIActionPlan, LiveScanner, and StackBuilder
- `scripts/migration-feature-flags.sql` defines the feature_flags table schema — NOT YET applied in Supabase (human action required)
- `.github/workflows/ci.yml` now runs Deno tests in CI — Deno edge tests are no longer drift-prone (CI catches regressions)
- `supabase/functions/stack-builder/index.ts` response schema changed: `plan: string` replaced with `steps[]`, `summary`, `assumptions[]`, `estimatedTotal`; `src/components/StackBuilder.jsx` now renders the structured shape and keeps fallback support for old `plan` payloads
- `src/observability.js` + `ObservabilityPanel.jsx` now derive AI usage / abuse pressure from recent AI `vault_events`
- `scripts/validate-launch-smoke.mjs` now checks the `/land/:creator` route guard, landing analytics, and `pg_ref` attribution
- `package.json` `build:cap` is Windows-safe and has been verified to emit `dist-cap`
- `context/PROJECT_STATUS.json` refreshed from stale S62 values to S64 repo truth
- Code remains ahead of production: S62 AI edge function updates (promo-advisor SSE, validate.ts import, stack-builder JSON schema) and S63 changes NOT YET deployed to Supabase
Public-safe summary only. Sensitive verification notes are maintained privately.
