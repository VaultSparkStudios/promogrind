<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-18 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 202.20
- Top 3: s90-command-ribbon · terms-and-deadline-promos · conflict-aware-tracker

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | UX / feature depth / feedback loop | 2h | 10 | 9 | 45.00 | **s90-command-ribbon** — S90 shipped counterfactual P&L and decision-journal engines, but Today still hides them. Put a compact command ribbon above Autopilot so operators see "what yesterday taught me" and "what the last 7 days would have paid" before adding volume. **Recipe:** wire `buildCounterfactualPnL` + `buildDecisionJournal` into `TodayDashboardPanel`; render only when useful, with no shame language and a sparse-history fallback; add dashboard tests for signal/no-signal states. |
| 2 | 🔥 | UX / speed / trust | 1h | 9 | 8 | 45.43 | **terms-and-deadline-promos** — Promo cards already show decay sparks, but the two highest-friction safety signals from S90 are invisible: "terms changed" and "execute before edge floor." **Recipe:** have `SmartPromoRecommender` call `recordTermsSnapshot` and `computeExecutionDeadline`, rendering a `TERMS CHANGED` pill and "execute within Xh" line without adding AI or network calls; tests cover drift and deadline copy. |
| 3 | 🔥 | security / UX / safety | 1h | 9 | 8 | 45.43 | **conflict-aware-tracker** — The tracker can show open exposure, but not when two live promos collide on qualifier or rollover rules. This is a real-money mistake preventer, not decoration. **Recipe:** derive active promo candidates from open bets/workflows, call `detectPromoConflicts`, render a warning panel and row chips for affected books; add tests for visible blocking conflict copy. |
| 4 | ⚡ | gamification / education / feature depth | 1h | 8 | 7 | 35.33 | **kelly-sandbox-profile** — Kelly replay exists as a library but has no operator-facing learning loop. **Recipe:** add a Profile section comparing quarter/half/full Kelly against settled bet history using `compareKellyFractions`; show net/ROI/samples and hide when sample is empty; tests cover section math via existing library. |
| 5 | ⚡ | engagement / feedback loop | 1h | 7 | 7 | 30.92 | **share-briefing-button** — The zero-PII share-card data model exists, but operators cannot generate the card from Today. **Recipe:** add a Share Briefing action in `TodayDashboardPanel` using `buildShareCardData` + `renderShareCardCanvas`; copy/share a PNG where supported and prove the payload remains PII-safe through the library tests. |
| 6 | 💡 | token cost / organization / process | 30m | 5 | 6 | 30.09 | **execution-doc-noop-guard** — Recent audits are easy to rerun and re-implement twice. **Recipe:** write `docs/IMPLEMENT_PLAN.md` for S91 with explicit shipped/deferred accounting and append an execution log to this audit so future `/implement` runs can skip completed items. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- s90-command-ribbon
- terms-and-deadline-promos
- conflict-aware-tracker

## Skipped (Priority < 1.5)

- 0 items deferred.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

| # | Slug | Status | Evidence |
|---:|---|---|---|
| 1 | s90-command-ribbon | shipped | `TodayDashboardPanel` now renders `OperatorCommandRibbon` from `buildCounterfactualPnL` + `buildDecisionJournal`; sparse-history fallback included. |
| 2 | terms-and-deadline-promos | shipped | `SmartPromoRecommender` now surfaces `TERMS CHANGED` via `recordTermsSnapshot` and edge-floor deadlines via `computeExecutionDeadline`. |
| 3 | conflict-aware-tracker | shipped | `Tracker` derives active promo candidates, calls `detectPromoConflicts`, renders guard panel + per-book `CONFLICT` chips. |
| 4 | kelly-sandbox-profile | shipped | `ProfilePanel` now renders `KellySandboxSection` with quarter/half/full Kelly replay from settled history. |
| 5 | share-briefing-button | shipped | Today briefing now generates a zero-PII canvas share card through `buildShareCardData`, `assertShareCardPiiSafe`, and `renderShareCardCanvas`. |
| 6 | execution-doc-noop-guard | shipped | `docs/IMPLEMENT_PLAN.md` written for S91 and this execution log records all audit items. |

Verification: `npm test -- dashboard.test.js` passed 13/13; `npm test -- promoConflict.test.js` passed 3/3; `npm run verify:launch-local` passed end to end with 450/450 tests, AI usage ledger, hook guard, auth/launch/UX/browser smokes, public dist exposure, replay proofs, bundle budget, and strict public sanitization.
