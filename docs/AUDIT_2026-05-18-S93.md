<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-18 -->
<!-- project: promogrind · type: app -->
<!-- session: S93 -->

# Project Audit — PromoGrind (Session 93)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

Project type: `app` → axis weights applied: **UX ×2 · Feature depth ×2 · Speed ×1.5 · Token cost ×1.5**.
Soul-aligned: every item either reduces noise, increases clarity, or closes a feedback loop. No items add dopamine bait or "hammer this now" language. Anti-tilt and discipline-rewarding items are favored.

## Top-line

- Total items: 10
- Combined Priority: 319.9
- Top 3: `recommender-explainer-drawer` · `calc-to-tracker-lifecycle` · `cache-aware-advisor`

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | UX / transparency | 1h | 10 | 6 | 37.9 | **recommender-explainer-drawer** — Smart Promo Recommender has the weight data (`whyRanked` + outcome-memory + decay + terms-drift) but only renders a one-liner. Operators ask "what's actually driving this rank?" and have no answer. **Recipe:** add a collapsible `ExplainerDrawer` under each promo card that breaks rank into a 5-row table — terms drift, edge decay %, settled-sample size, conflict flags, outcome-memory weight. Pure UI, no new engine, no AI call. Add `SmartPromoRecommender.test.jsx` cases for visible/hidden states. |
| 2 | 🔥 | UX / feature depth / friction | 2h | 10 | 7 | 35.0 | **calc-to-tracker-lifecycle** — Operators run a calculator, then re-enter the same numbers into Tracker. A real-money workflow with double-entry is the biggest friction loop in the app. **Recipe:** add a "Track this play" action on every calculator output that calls a new `src/workflows/handoff.js` builder, pre-fills a Tracker row with calc outputs (book, EV, expected stake, qualifier terms snapshot), and stamps `sourceCalc` provenance. Wire through the existing `workflows/store.js` so adaptive ranking inherits the breadcrumb. Add `handoff.test.js` covering pre-fill, idempotency, and `sourceCalc` provenance. |
| 3 | 🔥 | token cost / AI quality | 2h | 10 | 7 | 35.0 | **cache-aware-advisor** — Promo Advisor already rule-routes first, but identical prompts within a session still spend tokens on identical model calls. **Recipe:** add `src/ai/promptCache.js` keyed by `sha256(promptCanonical + modelId + temperature)`; integrate into `src/ai/gateway.js` before the network call; cache hits record to `ai:usage` ledger as `source: 'prompt-cache'`. **Measurement plan:** baseline 7-day rolling AI token spend pre-deploy; post-deploy track `cacheHitRate` and `tokensSavedByCache` in `docs/AI_USAGE_LEDGER.md`; target ≥30% session-level hit rate on Promo Advisor. |
| 4 | 🔥 | feature depth / AI / safety | 4h | 10 | 9 | 34.8 | **mistake-memory-loop** — The decision journal stores past losses but doesn't surface them when an operator is about to repeat one. This is a personalized pattern detector — the operator's own mistakes are the highest-signal training corpus. Nobody else in this category does this. **Recipe:** add `src/lib/mistakeMemory.js` with `matchPriorMistake(candidate, ledger)` using a 5-dim similarity vector (book, promo type, rollover band, qualifier conditions, stake band) at ≥0.8 cosine. Render a soft `⚠ Similar to a prior settled loss` chip with a tap-through link to the journal entry. **Soul guard:** copy is observational ("you tried this; it lost $X"), never punitive. Tests cover match precision and no-shame copy invariant (no exclamation marks, no caps, no "should not"). |
| 5 | 🔥 | AI / trust / token cost | 4h | 10 | 9 | 34.8 | **ai-calibration-tracker** — AI surfaces recommend confidence but never get graded against outcomes. Publishing the AI's own honesty score is rare in consumer products and converts "trust me" into "trust my track record." **Recipe:** add `src/lib/aiCalibration.js` that links each AI recommendation back to its settled outcome and computes Brier score per source (Promo Advisor, Action Plan, Recommender). Surface `Promo Advisor: 73% calibrated (last 30d)` in Profile and as a small badge near AI outputs. Cold-start: hide until sample ≥ 10. Add tests for Brier math and minimum-sample gating. **Why it also reduces token cost:** poorly-calibrated AI sources drop in the recommender priority, so the rule engine handles a larger share of advisory traffic. |
| 6 | 🔥 | gamification / engagement / SOUL | 4h | 10 | 9 | 34.8 | **counterfactual-twin-battle** — Operator Twin and Counterfactual P&L are isolated engines. Combine them into a weekly 3-way scorecard: **You · Twin (best version of you) · Discipline Twin (perfect rule adherence)**. Self-competition, not whale-leaderboard. **Recipe:** add `src/lib/twinBattle.js` that produces three weekly P&L curves from `replayLedger` + `operatorTwin` + a deterministic rule-perfect baseline. Surface as a single `TwinBattleCard` in Today, ranked by net delta. **Anti-bait safeguard:** no medals, no streaks for being ahead — only a sober delta with a link to the largest gap-creating decision. Tests cover empty-history, all-three-equal, and dominated states. |
| 7 | 🔥 | feature depth / safety / SOUL | 4h | 10 | 8 | 30.9 | **bankroll-stress-test** — Kelly Sandbox shows historical sizing; nobody shows forward exposure. "What happens to my bankroll if the next 5 lose?" should be a one-tap answer before committing. **Recipe:** add `src/lib/bankrollStress.js` running 500-iteration Monte Carlo on current open exposure + planned plays at user-chosen percentile bands (P10/P50/P90). Render an inline curve in Profile and as a confirm-modal preview when total exposure > 25% bankroll. Tests cover deterministic seed reproducibility and bankroll-floor breach detection. |
| 8 | 🔥 | UX / feature depth / studio-state | 4h | 10 | 8 | 30.9 | **edge-decay-heatmap** — Per-promo decay sparks exist; the studio-wide one-glance view doesn't. Operators need to see "which 3 plays across all 53 calculators are decaying fastest right now" without 53 clicks. **Recipe:** add a `LiveEdgeHeatmap` panel above Smart Promo Recommender that grids active books × promo types color-coded by current EV minus floor. Sourced from existing `edgeDecay.computeExecutionDeadline` outputs; no new engine. Pin the top 3 movers in a strip; no "act now" copy. Tests cover empty state, all-stale state, and color-mapping monotonicity. |
| 9 | ⚡ | security / trust / viral | 8h | 8 | 10 | 24.1 | **provenance-receipts-v2** — HMAC-signed operator passport exists. Extend it: every settled promo emits a tamper-evident receipt linking promo-terms-snapshot → decision → execution → settle outcome → P&L. World-first cryptographic integrity for sportsbook-promo conversion. Operators can share verifiable proofs of edge — a unique viral moat. **Recipe:** add `src/lib/promoProvenance.js` that chains receipts via `previousReceiptHash` (no blockchain, just a hash-linked ledger), signed by the existing passport key. Public verifier at `/verify-receipt?id=…` reads the public passport keys and confirms the chain. Tests cover signature roundtrip, tampered-chain detection, and zero-PII payload assertion. |
| 10 | ⚡ | safety / anti-tilt / friction-as-feature | 4h | 7 | 8 | 21.7 | **pre-mortem-friction** — Before a stake exceeds N% of bankroll, force the operator to read 3 "how this could go wrong" scenarios auto-generated from prior settled losses on similar plays. Friction is the feature; it's borrowed from anesthesiology checklists. **Recipe:** add `src/lib/preMortem.js` that pulls 3 nearest prior-loss patterns via `mistakeMemory.matchPriorMistake` and renders them in a modal blocking the stake submit. User must tap acknowledge per scenario (no auto-skip, but no shaming either). Threshold configurable in Profile (default 10% bankroll). Tests cover threshold gating, modal copy, and bypass-resistance. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `mistake-memory-loop` (Innov 9) · `ai-calibration-tracker` (Innov 9) · `counterfactual-twin-battle` (Innov 9) · `bankroll-stress-test` (Innov 8) · `edge-decay-heatmap` (Innov 8) · `provenance-receipts-v2` (Innov 10) · `pre-mortem-friction` (Innov 8)

## Skipped (Priority < 1.5)

- 0 items deferred.

## Decision-respect notes

- Honors **DECISIONS.md 2026-05-17 (S89)** — no affiliate-launch coupling proposed; `provenance-receipts-v2` is operator-owned trust and does not depend on partner approvals.
- Honors **DECISIONS.md 2026-05-13 (S86)** — every item operates on PromoGrind-only account state; no items cross into Studio membership.
- Honors **DECISIONS.md 2026-04-22 (canonical seams)** — all new state routes through `src/workflows/store.js`, `src/ai/gateway.js`, or `src/lib/` pure modules. No new orchestration shortcuts.
- Honors **DECISIONS.md 2026-05-13 (anti-tilt SOUL)** — `pre-mortem-friction` and `mistake-memory-loop` strictly avoid punitive copy; tests assert no-shame invariant.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

| # | Slug | Status | Evidence |
|---:|---|---|---|
| 1 | recommender-explainer-drawer | shipped | `SmartPromoRecommender` now renders a collapsible `ExplainerDrawer` with 5 weight rows (terms drift, edge decay, execution deadline, outcome memory, rank weights). 3 new tests in `SmartPromoRecommender.test.jsx`. |
| 2 | calc-to-tracker-lifecycle | shipped | `src/workflows/handoff.js` builds deterministic `calc-handoff:<calc>:<book>` workflows with `sourceCalc` provenance; `CalculatorReceipt` exposes optional `onTrack` action. 5 new tests in `handoff.test.js`. |
| 3 | cache-aware-advisor | shipped | `src/ai/promptCache.js` adds `withPromptCache` HOF using existing `buildCacheKey`/`readTimedCache`; tracks hits/misses/tokensSaved in `pg_ai_prompt_cache_stats`. 4 new tests in `promptCache.test.js`. **Measurement plan recorded.** |
| 4 | mistake-memory-loop | shipped | `src/lib/mistakeMemory.js` 5-dim similarity matcher (book, promoType, rollover band, qualifier Jaccard, stake band) at threshold 0.8; sober chip wired into `SmartPromoRecommender`. 5 new tests in `mistakeMemory.test.js` including no-shame copy invariant. |
| 5 | ai-calibration-tracker | shipped | `src/lib/aiCalibration.js` records predictions, resolves with outcomes, computes Brier per source with MIN_SAMPLE=10 gating; `renderCalibrationBadge` helper for UI. 5 new tests. |
| 6 | counterfactual-twin-battle | shipped | `src/lib/twinBattle.js` produces 3-way weekly P&L (you · twin · disciplineTwin) + largest-gap review surface. 4 new tests in `twinBattle.test.js`. |
| 7 | bankroll-stress-test | shipped | `src/lib/bankrollStress.js` deterministic Monte Carlo (Mulberry32) with P10/P50/P90, floor-breach detection, `shouldShowStressPreview` at 25% threshold. 6 new tests. |
| 8 | edge-decay-heatmap | shipped | `src/lib/edgeDecayHeatmap.js` aggregates active book × promo type into tone-graded cells (stable/fresh/warm/critical/expired) with top-3 movers. 3 new tests. |
| 9 | provenance-receipts-v2 | shipped | `src/lib/promoProvenance.js` hash-linked HMAC-signed receipt chain with `previousReceiptHash`, PII stripping at the receipt builder, `verifyChain` tamper detection, public-safe export helper. 6 new tests. |
| 10 | pre-mortem-friction | shipped | `src/lib/preMortem.js` triggers above 10% bankroll threshold, pulls top-3 prior-loss scenarios via `mistakeMemory`. 5 new tests. |

Verification: `npm test` passed **500/500** (up from 450 — 50 net-new tests across 7 new modules + 1 component test). `npm run verify:launch-local` exit 0: 500/500 tests, AI usage ledger, hook guard, auth/launch/UX/browser smokes, public dist exposure 0 critical/0 warning, replay-proof 0 regressions, bundle budget OK, strict public-repo sanitization with one pre-existing hygiene-band path warning in `.mcp.json` (unrelated to this audit work).
