<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-25.json; do not hand-edit -->
<!-- generated-at: 2026-07-26 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 110.94
- Top 3: Make launch proof completion require a criterion-level evidence quorum · Turn launch credentials into target-bound, scope-verified capabilities · Make every startup signal declare provenance, availability, and semantic state
- Sampled: 18/545 files · 30000 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | fire | security / speed / observability / token reduction | 6h | 10 | 9 | 30.0 | · | **target-bound-capability-truth-plane** — Turn launch credentials into target-bound, scope-verified capabilities.  **Recipe:** Build a public-safe launch capability contract whose checks resolve only through the control-plane secrets gateway, bind every capability to its intended project/domain, and report missing/present/authenticated/authorized as distinct states. Add read-only probes for Cloudflare response-header rules, Brevo sender-domain authentication, PromoGrind Supabase project access, Stripe account mode, and browser-safe capture configuration. Emit a redacted JSON receipt consumed by launch-local and the external proof ledger; never print keys or treat presence as readiness. |
| 2 | fire | feature depth / feedback loop / security / release UX | 5h | 10 | 9 | 32.1 | · | **launch-proof-evidence-quorum** — Make launch proof completion require a criterion-level evidence quorum.  **Recipe:** Version the launch-proof schema with stable criterion IDs and typed receipts. Derive status from criterion coverage instead of writing status directly; require each receipt to name criterion, source, target, observedAt, redacted evidence, and verifier; reject unknown criteria, duplicates, stale targets, unsafe payloads, and manual generic completion. Migrate existing evidence honestly, update all three proof runners and ledger counts, and add adversarial tests proving incomplete or fabricated receipts cannot produce complete. |
| 3 | fire | observability / UX / speed / organization | 4h | 8 | 8 | 24.8 | · | **startup-brief-provenance-lattice** — Make every startup signal declare provenance, availability, and semantic state.  **Recipe:** Introduce a compact signal-state lattice (pass/warn/fail/empty/unavailable/skipped/stale) and source precedence contract. Project identity must come from PROJECT_STATUS with cache metadata only as enrichment; 0/0 compliance is unavailable rather than 0%; absent portfolio surfaces render public-shim unavailable instead of zero; Ark count must use the drained local receipt/cache source when portfolio logs are absent. Add golden tests for all states and regenerate the brief. |
| 4 | high | security / speed / automation | 3h | 8 | 7 | 24.1 | · | **streaming-secret-archaeology** — Make full-history secret scanning bounded, streaming, and precision-aware.  **Recipe:** Replace O(commits) subprocess fan-out with one bounded git log --format/--patch stream, parse commit/file/date markers in-process, deduplicate findings, exclude known lockfile integrity fields, and require contextual key labels plus entropy for generic 40-character candidates. Add fixture-based true/false positives, progress and timeout controls, JSON schema stability, then run the full history scan directly and classify every residual finding. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### target-bound-capability-truth-plane
- **L1 · minimal** _(~2h)_ — Add target metadata and tri-state read-only checks for the five current capabilities.
- **L2 · solid** _(~4h)_ — L1 plus redacted JSON receipts, deterministic fixtures, and launch-ledger integration.
- **L3 · genius** _(~6h)_ — L2 plus scope-specific HTTP probes, freshness/target invariants, and Ark-ready remediation payloads for control-plane gaps.

### launch-proof-evidence-quorum
- **L1 · minimal** _(~1.5h)_ — Add criterion IDs and derive status from explicit satisfied criteria.
- **L2 · solid** _(~3h)_ — L1 plus typed receipt validation and migrate the proof runners/ledger.
- **L3 · genius** _(~5h)_ — L2 plus freshness/target/verifier invariants, safe migration, adversarial tests, and generated blocker reconciliation.

### startup-brief-provenance-lattice
- **L1 · minimal** _(~1h)_ — Correct project identity precedence and 0/0 compliance semantics.
- **L2 · solid** _(~2.5h)_ — L1 plus public-shim availability states for orchestrator and Ark signals.
- **L3 · genius** _(~4h)_ — L2 plus reusable signal-state helpers, provenance annotations, and golden regression coverage.

### streaming-secret-archaeology
- **L1 · minimal** _(~1h)_ — Collapse scanning to one git stream and skip lockfile integrity hashes.
- **L2 · solid** _(~2h)_ — L1 plus context/entropy validation and deterministic fixtures.
- **L3 · genius** _(~3h)_ — L2 plus bounded progress, dedupe, schema contract, and a verified full-history run.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- target-bound-capability-truth-plane
- launch-proof-evidence-quorum
- startup-brief-provenance-lattice
- streaming-secret-archaeology

## Skipped (Priority < 1.5)

- upgrade-every-outdated-package — Rejected on live verification: only patch/minor updates are wanted and Dependabot plus auto-merge already own that path; major React/Vite upgrades require a separate migration premise, not freshness theater.
- decompose-every-file-over-500-lines — Rejected as metric-only: 13 files exceed 500 lines, but no failing responsibility, performance, or test-isolation boundary was demonstrated during the capped audit.
- add-client-side-security-headers — Rejected as technically false: browser code and tracked GitHub Pages _headers cannot repair delivered response headers. The proxied edge needs Zone Transform Rules scope, which every current credential probe returns 403 for.
- rewrite-all-stale-propagated-scripts — Rejected as a false age premise: most >90-day scripts are public-repo protocol shims or stable one-shot migrations; age alone is not debt. Only the scanner with reproduced timeouts survived.
- consolidate-all-task-board-parsers — Rejected for now: duplicate parsers exist, but the live genius cache correctly regenerated to zero and no divergent parse result was reproduced.

---
*Infrastructure-first rubric requested by the founder, merged with the public-app release lens and audit personalization weights. No item reverses recent decisions or treats external proof as local completion.*
