<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-07-01 -->
<!-- project: PromoGrind · type: app -->

# Project Audit — PromoGrind S110

> One ranked improvement plan covering verified repo-owned work. Primary genius cache is empty; this pass expands through live innovation-pack evidence and keeps external launch proofs as honest deferrals.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 40.7
- Top 3: safe-spawn-helper-convergence · startup-context-meter-extraction · external-launch-proof-ledger

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | high | security / automation / developer-experience | 30m | 9 | 4 | 27.2 | **safe-spawn-helper-convergence** — Three public-safe closeout helpers directly import `node:child_process` even though PromoGrind already owns `scripts/lib/safe-spawn.mjs`. Switch them to the wrapper and verify the window-storm guard remains enforceable. **Recipe:** replace the imports in `scripts/closeout-step-3-7-parallel.mjs`, `scripts/record-skill-cost.mjs`, and `scripts/session-floor.mjs`; run `node scripts/check-windows-hide.mjs` plus helper smokes. |
| 2 | medium | speed / organization / observability | 2h | 7 | 3 | 10.5 | **startup-context-meter-extraction** — `scripts/render-startup-brief.mjs` remains 1354 lines, and its context-meter logic is pure enough to extract without changing behavior. This reduces renderer risk around an observability-critical block. **Recipe:** create `scripts/lib/startup-context-meter-block.mjs`, import it from the renderer, add focused tests, and verify brief golden/format checks. |
| 3 | external | truth / launch-readiness | external | 10 | 1 | 3.0 | **external-launch-proof-ledger** — Six launch-proof gates still require real mailbox, payment, tester, provider, or deploy-config evidence. Keep them visible and explicitly deferred; do not fabricate proof to pass a gate. **Recipe:** log the deferral in implementation/closeout and preserve the proof gates in task/status surfaces. |

## Innovation Reserve

- `startup-context-meter-extraction` is the strongest second-order candidate because it compounds prior renderer decomposition without touching user-facing product flows.

## Skipped

- Production auth email, Stripe, friend-beta, Brevo forwarding, Studio Ops Supabase capability, and capture-key proof are skipped as code work because they require real external evidence.

## Execution Log

| Item | Outcome | Evidence |
|---|---|---|
| safe-spawn-helper-convergence | shipped | `scripts/closeout-step-3-7-parallel.mjs`, `scripts/record-skill-cost.mjs`, and `scripts/session-floor.mjs` now import `spawnSync` from `scripts/lib/safe-spawn.mjs`; `node scripts/check-windows-hide.mjs` and helper smokes passed. |
| startup-context-meter-extraction | shipped | Added `scripts/lib/startup-context-meter-block.mjs`, moved live/fallback context-meter loading out of `scripts/render-startup-brief.mjs`, and added regression coverage for live payload normalization plus heuristic fallback. `node scripts/render-startup-brief.mjs`, `node scripts/validate-brief-format.mjs docs/STARTUP_BRIEF.md`, `npm test`, and `npm run verify:launch-local` passed. |
| external-launch-proof-ledger | honest-deferral | Real production auth email, Stripe, friend-beta, Brevo, Studio Ops Supabase capability, and capture-key proof still require external evidence. No proof was fabricated. |

## Verification Notes

- `node scripts/test-studio-script-regressions.mjs` and `node scripts/test-brief-golden.mjs` were attempted in-sandbox but hit the Windows sandbox `CryptUnprotectData` failure before execution. Escalated reruns were not approved, so their real exit codes remain unverified this session.
---
*Generated by `/audit` v1.0 · ready for `/implement`*
