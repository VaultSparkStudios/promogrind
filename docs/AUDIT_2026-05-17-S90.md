<!-- generated-by: /audit skill v1.0 (S90) -->
<!-- generated-at: 2026-05-17 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind (S90)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> Axis weights (app type): UX 2× · Feature 2× · Speed 1.5× · Token 1.5×.

## Top-line

- Total items: 12 ranked · 6 in innovation reserve
- Combined Priority: 528.6
- Top 3: `counterfactual-pnl-ribbon` · `decision-journal-autogen` · `terms-drift-detector`
- Session context: 430/430 tests, S89 just shipped 9 items (tilt-guard, causal explainer, edge-decay, twin, replay ledger, HMAC passport, calc pre-warm, AI budget self-binding). Audit deliberately avoids already-shipped surfaces and instead compounds them — every #1–#6 item layers on top of an S89 primitive.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | UX+Feature | 4h | 18 | 9 | 62.8 | **counterfactual-pnl-ribbon** — End-of-day ribbon: "You earned $X. Following AI's #1 pick → $Y. Skipping red-flagged → $Z." Compounds S89 replay ledger + outcome-memory into the single most retention-positive surface in this category — operators can finally *see* their edge or its absence in dollars. **Recipe:** Add `src/lib/counterfactualPnL.js` reading from `replayLedger` + settled results; render `CounterfactualRibbon` above `TodayDashboardPanel`; 3 tests covering positive/negative/empty-history cases. No new AI cost — pure deterministic replay. |
| 2 | 🔥 | Feature+AI | 2h | 14 | 8 | 56.0 | **decision-journal-autogen** — Auto-generate a 2-line daily decision journal at first morning load: "Yesterday you executed N promos (X settled W/L), skipped M (K turned hot), edge profile shifted by Δ." Stored locally, builds compounding operator memory. **Recipe:** `src/lib/decisionJournal.js` aggregates yesterday's actions from existing stores; surface card in `TodayDashboardPanel` collapsible by default; uses rule engine, zero AI tokens; 2 tests. |
| 3 | 🔥 | Feature | 6h | 16 | 10 | 53.3 | **terms-drift-detector** — When a sportsbook silently changes promo T&Cs mid-cycle (rollover, eligible markets, max payout), detect via hash diff of cached prior text and surface a "TERMS CHANGED" pill on that promo + audit-trail in trust receipts. Nobody in this category does this; it's the highest-trust operator move available. **Recipe:** `src/lib/termsDrift.js` SHA-256 hashes promo text on first sight, compares on re-render; persist in localStorage; show pill in `SmartPromoRecommender` + log to trust-receipt ledger; 3 tests. |
| 4 | ⚡ | Feature | 4h | 16 | 8 | 49.6 | **edge-half-life-scheduler** — Use S89 `edgeDecay` data to compute each promo's edge half-life and emit a personal execution deadline ("Execute by Wed 14:00 or skip — projected edge drops below your 12% floor"). Turns the decay radar from passive viz into active scheduling. **Recipe:** Extend `src/lib/edgeDecay.js` with `computeExecutionDeadline(promo, userThreshold)`; surface inline in promo card; 2 tests for half-life math + threshold gating. |
| 5 | 🔥 | Feature | 8h | 16 | 10 | 48.2 | **swarm-confidence-badges** — When ≥3 operator passports (S89 HMAC infra) independently flag a promo as hot, emit a "SWARM ✓" badge (anonymous, zero-PII). Compounds passport network effect — every new operator strengthens every other operator's signal. Free viral moat. **Recipe:** Local-only first iteration: simulate via `src/lib/swarmConfidence.js` reading a passport-share opt-in flag; structure code so a future CF Worker aggregator can swap in; render badge in `SmartPromoRecommender`; 4 tests + 1 schema doc. Server piece deferred to dedicated session. |
| 6 | ⚡ | Feature | 6h | 16 | 9 | 48.0 | **promo-recipe-synthesis** — Deterministic 7-day execution plan generator: given bankroll, history, and open promos, produce an ordered sequence ("Mon: Promo A, Tue rest, Wed: Promo B…") respecting Kelly fraction + tilt-guard cooldowns + decay deadlines. **Recipe:** `src/lib/promoRecipe.js` planner (greedy + EV-density sort, no LLM); `PromoRecipePanel` rendered in `Today`; 4 tests including bankroll-cap and cooldown respect. |
| 7 | ⚡ | UX | 8h | 18 | 8 | 43.4 | **ocr-settlement-paste** — User pastes confirmation screenshot → auto-extract bet amount/outcome/book → pre-fill settlement form. Kills the #1 friction in the tracker loop. **Recipe:** Use Tesseract.js (client-side, no AI cost) with a per-book template (DraftKings/FanDuel/etc.); add paste handler in `Tracker`; 3 tests with fixture images. Gate behind `navigator.clipboard.read` permission; lazy-load Tesseract to avoid bundle bloat. |
| 8 | ⚡ | UX | 3h | 14 | 7 | 42.2 | **promo-conflict-detector** — Warn when user has two active promos on same book/market whose rollover requirements are mutually exclusive. Prevents unforced operator errors. **Recipe:** `src/lib/promoConflict.js` rule set (book + market + requirement-class); render warning chip in `Tracker` open-bet list; 3 tests covering known conflict patterns. |
| 9 | 💡 | Feature | 3h | 14 | 7 | 42.2 | **bankroll-kelly-sandbox** — Interactive slider on Profile: "What if you had used 0.25 / 0.5 / 1.0 Kelly fraction over last 90 days?" Replay over user's own settled history. **Recipe:** Add `simulateKellyFraction(history, k)` in `src/lib/kellySim.js`; `KellySandbox` component in Profile; 3 tests. Educational + visceral. |
| 10 | 💡 | UX+Engagement | 3h | 12 | 8 | 41.4 | **operator-briefing-share-card** — One-tap "Share daily briefing" → renders a zero-PII PNG (canvas) with discipline score, lane mastery, edge headline. Free organic loop via X/Discord. **Recipe:** `src/lib/shareCard.js` (canvas → blob → navigator.share/clipboard fallback); button on `TodayDashboardPanel`; 2 tests verifying no PII fields included. |
| 11 | ⚡ | Speed | 3h | 12 | 6 | 31.0 | **calculator-lazy-route-split** — 53 calculators currently in main bundle. Lazy-load per route + S89 pre-warm prefetches predicted top-3. Likely 30-50% TTI reduction on cold load. **Recipe:** Convert calculator imports in `src/App.jsx` to `lazy(() => import(...))`; wrap routes in `<Suspense>`; integrate with `calcPreWarm`; measure with `bundle:budget` before/after; expected `dist/assets/App-*.js` shrink ≥150KB. |
| 12 | 💡 | Token | 6h | 10.5 | 8 | 28.0 | **ai-cost-crash-diet** — Three-layer cut: (1) deterministic prompt-hash cache (already partial — extend coverage), (2) precompute top-20 static promo analyses at build time, (3) route 80% of queries through local rule engine first. **Recipe:** Extend AI gateway with `src/ai/promptCache.js` (SHA-256 prompt key → cached response); add `scripts/precompute-promo-analyses.mjs` (build-time); gate gateway on cache hit; measure via `docs/AI_USAGE_LEDGER.md` 7-day delta; target ≥60% token reduction. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `swarm-confidence-badges` (10) — network effect compounds passport infra
- `terms-drift-detector` (10) — zero competitors do this; pure trust play
- `counterfactual-pnl-ribbon` (9) — retention-defining surface
- `promo-recipe-synthesis` (9) — converts deterministic signal into action plan
- `decision-journal-autogen` (8) — compounding operator memory
- `ocr-settlement-paste` (8) — removes biggest friction in tracker loop

## Skipped (Priority < 1.5)

- *Squad/community credibility system* — already on TASK_BOARD Later; depends on swarm-confidence landing first; revisit after #5.
- *Bankroll orchestration layer* — already on TASK_BOARD Later; partial coverage via #9 Kelly sandbox.
- *App.jsx full decomposition finale* — already on TASK_BOARD Next; structural, not strategic — let it ride as background task.

## Implementation sequencing rationale

Execute in priority order with one swap: **#11 calculator-lazy-route-split** lands FIRST. Reason: bundle shrink improves every subsequent UX surface's cold-load metric and has zero semantic dependency on other items. After that, take items in priority order — they layer cleanly (passport → swarm; edge-decay → half-life; replay → counterfactual; outcome memory → decision journal).

---

## Execution Log (S90 sprint)

| # | Slug | Status | Files | Tests |
|---|---|---|---|---|
| 1 | counterfactual-pnl-ribbon | ✓ shipped (core) | `src/lib/counterfactualPnL.js` | 3 |
| 2 | decision-journal-autogen | ✓ shipped (core) | `src/lib/decisionJournal.js` | 3 |
| 3 | terms-drift-detector | ✓ shipped (core) | `src/lib/termsDrift.js` | 3 |
| 4 | edge-half-life-scheduler | ✓ shipped | `src/lib/edgeDecay.js` (extended) | 3 (added to existing 4) |
| 5 | swarm-confidence-badges | deferred | — | — |
| 6 | promo-recipe-synthesis | deferred | — | — |
| 7 | ocr-settlement-paste | deferred | — | — |
| 8 | promo-conflict-detector | ✓ shipped (core) | `src/lib/promoConflict.js` | 3 |
| 9 | bankroll-kelly-sandbox | ✓ shipped (core) | `src/lib/kellySim.js` | 3 |
| 10 | operator-briefing-share-card | ✓ shipped (core) | `src/lib/shareCard.js` | 2 |
| 11 | calculator-lazy-route-split | deferred (S91 dedicated session) | — | — |
| 12 | ai-cost-crash-diet | deferred (S92 — wait for ledger baseline) | — | — |

- **Shipped:** 7 / 12 items (deterministic core modules + tests)
- **Deferred:** 5 / 12 (rationale in IMPLEMENT_PLAN.md)
- **Tests:** 450 / 450 passing (was 430 — +20 net-new across 7 new modules + 3 extending edgeDecay)
- **AI cost:** $0 added (all items use rule engine / local computation)
- **UI wiring:** core modules ready for thin component integration in S91

*Generated by `/audit` v1.0 · executed by `/implement` v1.0*
