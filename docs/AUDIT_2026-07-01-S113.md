<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-07-01 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind (S113)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order. Sidecar: `docs/AUDIT_2026-07-01-S113.json` (source of truth).

## Top-line

- Total items: 7 (+2 rejected on verification)
- Combined Priority: 104.6
- Top 3: intelligence-index · operator-data-vault · edge-decay-heatmap-wiring

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | feature-depth | 4h | 8 | 8 | 24.8 | **intelligence-index** — Operator Command Deck: one surface indexing all ~15 built operator-intelligence libs with live personal status. They're fully built + tested but hidden as conditional dashboard widgets; users can't see what the operator system knows. **Recipe:** lazy Track route, card per module: decision-line + live status from the module's own lib + deep link. Pure reuse, zero new deps. |
| 2 | 🔥 | feature-depth | 4h | 9 | 7 | 24.4 | **operator-data-vault** — versioned export envelope + validated import/restore. `dataControls.js` has export+clear but NO import anywhere in src; `storage.js` has zero schema versioning — portability is one-directional. **Recipe:** envelope (schemaVersion, integrity digest via WebCrypto) + `importLocalDataExport` with strict validation, merge/replace, dry-run preview; Restore UI in ProfilePanel. |
| 3 | 🔥 | feature-depth | 2h | 8 | 6 | 24.0 | **edge-decay-heatmap-wiring** — `lib/edgeDecayHeatmap.js` (S93) is imported only by its own test; the feature never reached users. **Recipe:** render in TrackInsights with theme-aware intensity cells, aria grid, empty state, component test. |
| 4 | ⚡ | ux | 4h | 7 | 4 | 10.8 | **calculator-a11y-pass** — 20/21 calculator files have zero aria attributes; results announce nothing. **Recipe:** labelled Field + live-region Result primitives in ui.jsx, sweep the fleet, representative render tests. |
| 5 | 💡 | speed | 2h | 5 | 3 | 7.5 | **dashboard-memoization** — heavy `today.js` composition + per-render style objects in Ledger/UserMenu. **Recipe:** probe first (no phantom fix), memoize composition, hoist static styles, measure. |
| 6 | ⚡ | process | 4h | 6 | 3 | 7.0 | **component-render-tests** — only 2/62 test files are component tests; the five largest components have none. **Recipe:** smoke-level render/interaction tests for TodayDashboardPanel, ProfilePanel, UserMenu, Ledger. |
| 7 | 💡 | speed/security | 30m | 4 | 2 | 6.1 | **public-repo-hygiene** — dead `src/theme.js` (zero importers), git-tracked root screenshot in a public repo, untracked zip cruft. **Recipe:** git rm screenshot, delete theme.js, gitignore root zips, suite green. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- intelligence-index (8)

## Skipped / rejected on verification (recorded WINS)

- **command-palette** — premise false: Ctrl+K/Cmd+K calc search already exists (`src/App.jsx:156`).
- **enable-feature-flags** — flags OFF is deliberate launch gating tied to the 6 external proof gates, not a defect.

## Honest deferrals (external evidence required — unchanged from S112)

production auth email · Stripe smoke purchase · friend beta · Brevo forwarding · Studio Ops Supabase capability · production capture public-key proof

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

| Seq | Slug | Rung | Result |
|---|---|:-:|---|
| 1 | public-repo-hygiene | L3 | SHIPPED `7a0d755` — dead theme.js + tracked screenshot removed; untracked duplicate root deploy guides deleted (docs/ copies canonical); root *.zip already gitignored, local zips left untouched. |
| 2 | edge-decay-heatmap-wiring | L2 | SHIPPED `36a5d48` — panel in TrackInsights with tone summary, movers, aria grid; copy corrected to match lib's default daily decay window; 5 tests. |
| 3 | operator-data-vault | L2 | SHIPPED `b7784ef` — BONUS root-fix: inventory tracked phantom keys (pg_app_data, pg_compact_mode) so exports silently omitted the core promo_engine_v3 blob; real key surface now tracked with entitlement/attribution/queue keys deliberately excluded; schemaVersion + fnv1a32 digest + fail-closed import (merge/replace/dry-run) + ProfilePanel Restore UI; 13 tests. |
| 4 | intelligence-index | L2→L3 | SHIPPED `86dd486` — Operator Command Deck route (Track): 13 modules, attention-ranked (act>live>idle), decision-line + live status from each module's own lib + coach copy + deep links; L3 attention ranking included; 8 tests. |
| 5 | calculator-a11y-pass | L2 | SHIPPED `0c87b41` — premise honestly demoted: shared In atom already labels inputs; real gap was result announcement. All 21 S.res result panels now role=status aria-live=polite; LineShop raw inputs labelled; 3 tests. |
| 6 | dashboard-memoization | L2 | SHIPPED `6af89c6` — probe-first verified 3 real hotspots: DailyDashboard unmemoized full snapshot, SmartPromoRecommender memo defeated by fresh Date dep (never hit), Ledger 91-day grid rebuilt per render. All memoized on data + calendar day. |
| 7 | component-render-tests | L2 | SHIPPED `35d1f96` — smoke render/interaction tests for TodayDashboardPanel, ProfilePanel (incl. full restore flow), UserMenu, Ledger; 7 tests. |

Suite: 62 files/511 tests → 66 files/545 tests. All items verified green before commit.
