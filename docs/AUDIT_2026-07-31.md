<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-31.json; do not hand-edit -->
<!-- generated-at: 2026-08-01 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 157.90
- Top 3: sober-engagement-contract · probabilistic-ai-calibration-loop · strict-validation-receipts
- Sampled: 20/596 files · 30000 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | fire | gamification / engagement / soul fidelity | 8h | 10 | 10 | 30.1 | ✓ | **sober-engagement-contract** — Replace outcome and login compulsion with a discipline-first engagement system. The product currently rewards profitable days, raw login streaks, betting volume, daily XP, fire badges, a comeback bonus, and urgency upsells. Those mechanics conflict with the explicit SOUL: calm operator discipline rather than casino-style dopamine or activity-as-edge. **Recipe:** Define discipline days from evidence-bearing actions regardless of profit direction: a settled outcome with an actual value, a reasoned skip, or a logged reviewed result. Replace profit/login streak language with review cadence, remove XP/bonus/urgency pressure from the dashboard, retone achievements toward verified decisions and learning, and add a source-level SOUL regression that rejects the displaced dopamine patterns. |
| 2 | fire | Artificial Intelligence / feedback loop / feature depth | 8h | 9 | 10 | 27.1 | ✓ | **probabilistic-ai-calibration-loop** — Turn Promo Advisor calibration into a semantically valid, automatically resolved learning loop. The app advertises calibration but records opportunity attractiveness as if it were outcome probability, then never resolves a prediction in production. The badge can only appear in synthetic tests. **Recipe:** Version the advisor receipt with an explicit positiveOutcomeProbability and probabilityBasis, normalize and bound both rule and model responses, refuse calibration when the field is absent, preserve the prediction identifier on the workflow, resolve it from a settled real outcome, expose resolved/unresolved coverage, and test the complete advisor-save-settle-summary path without a model call. |
| 3 | fire | observability / security / release engineering | 3h | 9 | 7 | 27.1 | ✓ | **strict-validation-receipts** — Make launch validation counts prove completion instead of merely resembling a fraction. Launch state recognizes any digit fraction as passing after a shallow word scan, so zero or partial execution can render green. **Recipe:** Add a structured parser that returns signal, observed count, expected count, and reason. A counted suite is green only when expected is positive and observed equals expected; zero, partial, contradictory, stale, or malformed evidence stays non-green. Preserve the wrapper API, attach parsed evidence to resolved validation rows, and add an adversarial matrix. |
| 4 | fire | feature depth / organization / observability | 6h | 10 | 8 | 26.7 | ✓ | **canonical-realized-outcome-contract** — Unify every learning surface on the actual realized outcome field. The core outcome feedback loop stores actualProfit, but multiple high-value learning modules silently read legacy aliases and calculate real settlements as zero. **Recipe:** Create one typed realized-outcome resolver with exact precedence actualProfit → profit → netProfit → outcome, explicit missing-vs-zero semantics, and finite-number normalization. Migrate every result-feedback consumer, preserve ledger compatibility, add contract vectors and a production-shaped end-to-end fixture, then ban new ad-hoc alias chains in the integrity gate. |
| 5 | fire | security / public trust / international UX | 7h | 10 | 8 | 25.2 | ✓ | **multisurface-public-claim-ratchet** — Close quantified-income and Edge-copy escape hatches in the multilingual public-claims gate. The public claims gate reports green while live pages state typical monthly earnings, guaranteed first-month ranges, pure profit, money that cannot be lost, and outcome-independent collection. Edge-generated advisor copy also escapes the scan. **Recipe:** Extend the locale-aware contract to evidence-sensitive quantified typical-income, recurring-profit, pure-profit, no-loss, and guarantee-adjacent phrasing; scan user-visible Edge result strings; make fixtures include structured data and code literals; rewrite every new finding into labeled illustrative/model outputs with execution, void, limit, eligibility, and price-movement boundaries; keep exact mathematical hedge descriptions only when their assumptions are local and explicit. |
| 6 | high | UI / UX / feedback quality | 4h | 8 | 7 | 21.7 | ✓ | **accessible-high-integrity-outcome-capture** — Make the result-feedback loop accessible and refuse semantically incomplete records. The highest-leverage feedback surface has visible div labels but no programmatic field names, pill buttons without pressed state, a non-live status message, and permissive records that omit the very reason or numeric outcome later analytics depend on. **Recipe:** Use stable ids and semantic labels, decimal input modes, native pressed-state semantics, grouped legends, and an aria-live receipt. Validate a finite signed realized value for settlement, require a reason for skip, preserve editable drafts without duplicate workflow entries, and add keyboard/accessibility plus data-integrity component tests. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### sober-engagement-contract
- **L1 · minimal** _(~2h)_ — Make streaks count reviewed decisions regardless of profit and rename the visible labels.
- **L2 · solid** _(~5h)_ — L1 plus remove login-derived upsells/comeback bonuses and retone achievement copy.
- **L3 · genius** _(~8h)_ — L2 plus convert daily XP into a calm review checklist, add explicit discipline evidence/reasons, and launch-gate SOUL anti-patterns.

### probabilistic-ai-calibration-loop
- **L1 · minimal** _(~3h)_ — Stop treating opportunityScore as probability and resolve only explicit probabilities.
- **L2 · solid** _(~6h)_ — L1 plus Edge receipt fields, workflow linkage, and automatic settlement resolution.
- **L3 · genius** _(~8h)_ — L2 plus basis/provenance, unresolved-coverage telemetry, cache-contract versioning, and end-to-end tests.

### strict-validation-receipts
- **L1 · minimal** _(~1h)_ — Require equal positive counts before a fraction passes.
- **L2 · solid** _(~2h)_ — L1 plus contradiction/zero/malformed cases and tests.
- **L3 · genius** _(~3h)_ — L2 plus structured reason/count provenance on every validation row.

### canonical-realized-outcome-contract
- **L1 · minimal** _(~2h)_ — Teach the affected modules to read actualProfit first.
- **L2 · solid** _(~4h)_ — Extract one resolver and migrate every known consumer with compatibility tests.
- **L3 · genius** _(~6h)_ — L2 plus production-shaped cross-module vectors, missing/zero provenance, and a no-divergent-alias source invariant.

### multisurface-public-claim-ratchet
- **L1 · minimal** _(~2h)_ — Add missing high-risk phrases and repair the two verified pages.
- **L2 · solid** _(~5h)_ — L1 plus quantified typical-income rules and all-repo copy remediation.
- **L3 · genius** _(~7h)_ — L2 plus Edge-generated copy coverage, structured-data fixtures, contextual safe-case handling, and a source inventory report.

### accessible-high-integrity-outcome-capture
- **L1 · minimal** _(~1h)_ — Add labels, input modes, pressed state, and live status.
- **L2 · solid** _(~3h)_ — L1 plus finite settlement/required skip validation and focused tests.
- **L3 · genius** _(~4h)_ — L2 plus idempotent record updates, error focus, grouped semantics, and production-shaped workflow assertions.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- A discipline cadence that treats reasoned restraint and loss review as progress, decoupling engagement from profit variance.
- A probability-basis receipt that separates offer attractiveness, confidence, and calibrated positive-outcome probability.
- A public-claim risk inventory that scans both static pages and dynamic Edge-authored user copy.
- A single realized-outcome resolver that makes zero, missing, modeled, and actual values mechanically distinct.

## Skipped (Priority < 1.5)

- bundle-splitting-overhaul — Rejected on verification: the live module graph is already route-split and the bundle gate reports 179.1KB initial raw / 60.0KB gzip, within budget. The empty PostHog chunk warning is low impact and below the 1.5 priority floor.
- new-ai-cost-controls — Rejected on verification: rule-engine short circuiting, prompt caching, daily entitlement ceilings, usage telemetry, and a budget state already exist. No measured overrun premise was found; flat-rate model cost remains notional.
- new-marketing-consent-system — Rejected on verification: S120 already shipped affirmative, versioned, reversible consent and send-boundary enforcement.
- manual-intelligence-report-refresh-as-a-fix — Rejected as a root fix: doctor --loop already owns source-derived revenue and IGNIS remediation. The current 9-day warnings remain maintenance work for this session, not a new product system.
- obelisk-ui-only-auth-switch — Honest deferral: CANON-045 checker output is malformed and the identity capabilities are incomplete. A UI-only switch without a verifier, replay/expiry contract, and account-link semantics would be a broken security facade.
- fabricate-external-release-proofs — Rejected by evidence policy: headers, forwarding, credential rotation, Supabase deployment authority, and real mailbox/payment/tester receipts remain external facts and cannot be manufactured by repository edits.

## Execution Log

### sober-engagement-contract
- `session 121` · **shipped** · Cadence now derives only from actual settled values, reasoned skips, and realized ledger entries; losses and zero count equally while expected-only and visits do not. Login writes, points awards, streak upsells, comeback/XP pressure, fire copy, and profit-volume badges were removed or converted to evidence receipts. Seventy-four engagement, dashboard, and SOUL contract tests pass.

### probabilistic-ai-calibration-loop
- `session 121` · **shipped** · Opportunity score is no longer converted into probability. Receipt v3 accepts only an explicit bounded positive-outcome probability with basis; client cache v4 preserves the linked prediction id; Track settlement resolves it from canonical realized profit; summaries expose resolved and unresolved coverage. Focused client integration coverage and all 45 Edge tests pass.

### strict-validation-receipts
- `session 121` · **shipped** · Launch validation now carries raw evidence, observed/expected counts, signal, and reason. Only equal positive counts pass; zero, partial, over-counted, contradictory, stale, and malformed evidence stays non-green. Eight launch-state tests pass.

### canonical-realized-outcome-contract
- `session 121` · **shipped** · One authority-aware currency parser now normalizes actualProfit before workflow persistence and feeds replay, journal, counterfactual, mistake, twin, and tilt modules. Production-shaped cross-module vectors plus focused suites pass 25/25.

### multisurface-public-claim-ratchet
- `session 121` · **shipped** · The ratchet now scans 316 static, application, and Edge-authored files with 18 multilingual rules and adversarial fixtures. All 33 surfaced violations were remediated; unsupported aggregate/ranking pages were publicly corrected; the hard-coded income predictor became a zero-default user-input scenario planner. Claims and release-surface gates pass with a content-addressed receipt.

### accessible-high-integrity-outcome-capture
- `session 121` · **shipped** · The feedback card now exposes labeled decimal inputs, semantic choice groups, pressed states, live status, fail-closed skip and settlement validation, normalized signed outcomes, and stable record identity. Four production-shaped component tests pass.

---
*All six items are verified against live implementation. Twenty-one candidates were merged, disproven, or honestly deferred. No external proof, credential authority, or launch readiness was invented.*
