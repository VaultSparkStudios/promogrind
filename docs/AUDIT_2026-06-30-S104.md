# Audit 2026-06-30 S104 - PromoGrind

## Premise Check

- Startup sync: `git pull --rebase origin main` was already up to date.
- Context meter: CONTINUE with ~3% used after implementation.
- Live genius list: `node scripts/ops.mjs genius-list` returned 0 items.
- Public repo shim gap: `node scripts/ops.mjs innovation-pack` is unavailable, so the expansion pass used live Task Board/App.jsx evidence and recent route-ownership debt.
- Doctor: `node scripts/ops.mjs doctor --update-json` passed 12/12 with blockingFailing 0.

## Ranked Plan

| Rank | Priority | Category | Item | Live Evidence | Result |
|---:|---|---|---|---|---|
| 1 | High | Speed / organization | app-jsx-decomposition-finale | TASK_BOARD S103 follow-up; `src/App.jsx` still owned Promo Calendar, referral/team/account surfaces, onboarding, and dashboard action widgets. | Shipped: `src/App.jsx` now delegates those surfaces and passes a <1500 composition guard. |
| 2 | High | Speed / UX | lazy-route-split-refinement | Initial extraction reduced file churn but kept several route surfaces in the App chunk. | Shipped: Promo Calendar, Referral Hub, Team Accounts, and Competitor Comparison now lazy-load as route chunks. |
| 3 | Medium | Automation coverage | composition-boundary-hardening | Prior guard only covered earlier extracted surfaces and a <2400 ceiling. | Shipped: guard covers S104 surfaces and enforces <1500 using normalized line endings. |
| 4 | External proof | Launch evidence | auth/Stripe/friend-beta/Brevo proofs | PROJECT_STATUS blockers still require real mailbox/payment/tester/control-plane evidence. | Honestly deferred; no proof fabricated. |

## Implementation Log

- Extracted Promo Calendar, alert preferences, and promo ROI table into `src/components/PromoCalendar.jsx`.
- Extracted Referral Hub into `src/components/ReferralHub.jsx`.
- Extracted Team Accounts into `src/components/TeamAccounts.jsx`.
- Extracted Competitor Comparison into `src/components/CompetitorComparison.jsx`.
- Extracted dashboard action widgets into `src/app/DashboardActionWidgets.jsx`: PushEnableBtn, QuickAddBet, WeeklyGrindReport, BankrollWizard, and CopyMySetup.
- Extracted onboarding modal into `src/app/OnboardingWizard.jsx`.
- Converted the newly extracted route surfaces to React lazy chunks.
- Tightened `src/__tests__/appComposition.test.js` to keep those surfaces out of `src/App.jsx` and enforce <1500 lines.

## Verification

- `npx vitest run src/__tests__/appComposition.test.js` passed 2/2.
- `npm run check:hooks` passed.
- `npm test` passed 59 files / 508 tests.
- `npm run verify:launch-local` passed end to end: tests, AI usage ledger, hook-order, auth/launch/UX/browser smokes, dist exposure, proof replay, bundle budget, and strict public sanitization.
- Build output split route chunks for Promo Calendar, Referral Hub, Team Accounts, and Competitor Comparison; main App chunk is ~298.98 kB / 88.54 kB gzip after the lazy split.

## Honest Deferrals

- Production auth email proof still requires real mailbox evidence.
- Stripe smoke still requires a real purchase path.
- Friend beta still requires trusted tester evidence.
- Brevo contact forwarding and PromoGrind Supabase capability mapping remain Studio Ops/control-plane follow-ups.
