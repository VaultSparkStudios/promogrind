<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 130.62
- Top 3: operator-season-rail · self-serve-data-controls · public-dist-exposure-gate

## Audit Context

- Product type: app, weighted toward UX, feature depth, speed, and token/API cost.
- Current state: deployed/public-unlaunched operator system with 53 calculators, launch proof mirror, Operator Autopilot, trust receipts, discipline scoring, outcome-aware recommendations, and AI usage ledger.
- Constraints: do not invent affiliate proof, live Stripe proof, friend-beta proof, or cross-Studio membership behavior. Preserve PromoGrind-only account language until membership is proven.
- Main opportunity: turn the S87 proof/trust/operator work into a tighter launch-ready habit loop while hardening the browser bundle against public exposure regressions.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification / UX | 2h | 9 | 8 | 28.00 | **operator-season-rail** — Add a 14-day discipline season above daily missions that rewards closed loops, feedback coverage, and bankroll context instead of bet volume. This makes engagement feel like operator training, not casino bait. **Recipe:** create `src/lib/seasons.js`, surface the active season in `DailyMissionsPanel`, and add tests proving open volume alone does not create elite season progress. |
| 2 | 🔥 | security / trust / UX | 2h | 9 | 7 | 24.50 | **self-serve-data-controls** — Give Profile a compact export/clear-control surface for local PromoGrind data and analytics preference visibility. Users already see trust receipts; this gives them an immediate control loop. **Recipe:** add `src/lib/dataControls.js`, render `DataControlsSection` in `ProfilePanel`, and cover export summary / clear plan logic with tests. |
| 3 | 🔥 | security / speed | 2h | 9 | 7 | 24.50 | **public-dist-exposure-gate** — Fail launch verification if built browser assets contain admin-only proof fields, service-role-like strings, local paths, `.env` filenames, or private Studio markers. S87 created a generated proof mirror; this keeps the generated public bundle honest. **Recipe:** add `scripts/check-public-dist-exposure.mjs`, wire it into `verify:launch-local` after browser smoke, and test scanner rules on fixtures. |
| 4 | ⚡ | feedback loop / organization | 1h | 8 | 6 | 24.00 | **friend-beta-feedback-summary** — Convert friend-beta runner evidence into a durable `docs/BETA_FEEDBACK.md` summary with issue tags by friction type. This turns the required manual pass into backlog signal instead of a binary launch checkbox. **Recipe:** extend `scripts/run-friend-beta-checklist.mjs` to classify failed-step notes and write a public-safe summary whenever `--record` is used. |
| 5 | ⚡ | token cost / AI / process | 1h | 7 | 6 | 29.62 | **ai-cost-contract-in-launch-gate** — Make the AI usage ledger part of the launch verification path so model-call avoidance stays visible after feature work. **Recipe:** add `npm run ai:usage` to `verify:launch-local` after tests, and log the measurement contract in the execution log. |

## Innovation Reserve (Innovation ≥ 8 — execute these even when busy)

- operator-season-rail

## Skipped (Priority < 1.5)

- Live sportsbook affiliate URL wiring — skipped because approved partner URLs remain operator-owned external proof.
- Live Stripe smoke purchase — skipped because it requires a real billing transaction/evidence run rather than repo code.
- Cross-Studio membership integration — skipped by standing S86 decision until the shared identity layer is proven.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

- **operator-season-rail** — SHIPPED. Added `src/lib/seasons.js`, rendered the Operator Season rail in `DailyMissionsPanel`, and covered season scoring with `src/__tests__/seasons.test.js`.
- **self-serve-data-controls** — SHIPPED. Added `src/lib/dataControls.js`, rendered Profile export/clear controls, and covered inventory/export/clear behavior with `src/__tests__/dataControls.test.js`.
- **public-dist-exposure-gate** — SHIPPED. Added `scripts/check-public-dist-exposure.mjs`, wired it into `verify:launch-local`, removed the legacy public `vault-sdk.js` asset/reference after the gate caught the bundled Supabase anon JWT and service-role marker, and verified rebuilt `dist` passes 0 critical / 0 warning.
- **friend-beta-feedback-summary** — SHIPPED. Extended `scripts/run-friend-beta-checklist.mjs` so recorded evidence also updates `docs/BETA_FEEDBACK.md` with friction tags.
- **ai-cost-contract-in-launch-gate** — SHIPPED. Wired `npm run ai:usage` into `verify:launch-local` and replaced the Supabase client in `render-ai-usage-ledger.mjs` with direct PostgREST fetch so the command exits cleanly on Windows.

Verification: `npm run verify:launch-local` passed end to end on 2026-05-17: 409/409 tests, AI usage ledger render, hook-order guard, auth smoke, launch smoke, UX route integrity, browser smoke/build, public dist exposure gate, bundle budget, and strict public-repo sanitization.
