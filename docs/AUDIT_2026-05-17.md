<!-- generated-by: /audit skill v1.0 (S89) -->
<!-- generated-at: 2026-05-17 -->
<!-- project: promogrind · type: app -->

# Project Audit — PromoGrind (S89)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> S88 items previously shipped — archived to `AUDIT_2026-05-17-S88-shipped.md`.

## Top-line

- Total items: 10
- Combined Priority: 302.69
- Top 3: anti-tilt-circuit-breaker · adversarial-receipt-replay · operator-twin

## Audit Context

- Product type: app — weighted UX (2×), feature depth (2×), speed (1.5×), token cost (1.5×).
- Current state: deployed, public-unlaunched. S87/S88 shipped Operator Autopilot, trust receipts, discipline score, outcome memory, AI usage ledger, Operator Season rail, self-serve data controls, public `dist/` exposure gate, friend-beta feedback summary. 409/409 tests passing.
- Constraints (DECISIONS.md respect): no invented affiliate URLs, no live Stripe proof fakery, no cross-Studio membership (S86), no casino dopamine bait (SOUL anti-goal). All new items must preserve "operator training, not gambling hype" voice.
- Main opportunity: the product has the world's most thoughtful operator scaffolding for promo arbitrage but still ranks promos via static heuristics, treats every session as identical, and lacks counterfactual feedback. The S89 audit attacks **temporal intelligence**, **counterfactual learning**, and **anti-tilt safety** — three axes no competitor occupies.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | UX / gamification / safety | 2h | 10 | 9 | 45.00 | **anti-tilt-circuit-breaker** — Detect rapid-fire calculator invocations + recent losing streak + high open-bet exposure → silently inject a soft "step away" trust receipt and demote impulsive promos for the next 30 min. Operator software that protects the operator is what makes PromoGrind a category of one. **Recipe:** add `src/lib/tiltGuard.js` computing `tiltScore` from settle outcomes, calc-launch cadence (sliding 15-min window), and `disciplineScore` exposure; wire a `TiltCircuitBreakerBanner` into `DailyDashboard` and let `SmartPromoRecommender` consume the demotion signal; tests prove the breaker triggers on 4-calc-in-2-min + losing streak and stays silent on healthy patterns. |
| 2 | 🔥 | feedback loop / AI / gamification | 2h | 9 | 9 | 40.50 | **adversarial-receipt-replay** — 14 days after each meaningful decision, surface a counterfactual: *"You skipped the BetMGM promo on 5/3. Settled players who took it averaged +$12 EV; settled players who skipped saved $8 in deposit risk."* Trains discipline empirically rather than rhetorically. **Recipe:** add `src/lib/replayLedger.js` that joins trust receipts + outcome ledger + adaptive ranking snapshot on a 14-day lag; render `ReplayInsightCard` in Profile (collapsed by default, expanded weekly); tests prove no replay generates without ≥14 days of evidence and that the card never shames a skipped action. |
| 3 | 🔥 | AI / feature depth / UX | 4h | 10 | 10 | 38.69 | **operator-twin** — A locally-derived shadow of the user's discipline curve that predicts (rule-engine first, AI second) when the operator is drifting from their seasonal baseline. Surfaces a one-line forecast on the dashboard *before* they pick a promo: *"Your discipline curve has dropped 12% this week — three of the last five sessions had stale opens >24h."* **Recipe:** add `src/ai/operatorTwin.js` that builds a 28-day rolling baseline from `seasons.js` + `disciplineScore` + outcome ledger, emits drift forecasts via deterministic rules first; only if an `ANTHROPIC_API_KEY` is configured AND ledger budget allows, request a one-line natural-language nudge (cached 24h); render in `TodayDashboardPanel` above Autopilot; tests cover baseline math, the rule fallback path, and the AI-cache invariant. |
| 4 | 🔥 | AI / UX / token cost | 2h | 8 | 8 | 32.00 | **causal-promo-explainer** — Replace heuristic-rank reasons with a 3-line counterfactual: *"Ranked #1 because removing your hot-lane signal would drop it to #5; lane mastery boost: +2; outcome-memory weight: +1."* No new AI cost — surface the ranking math we already compute. **Recipe:** extend `src/dashboard/today.js` to emit a `whyRanked` ablation summary per promo (compute rank with each weight set to zero, diff the position); render in `SmartPromoRecommender` as an expandable "why" line; tests cover the ablation math and that removing all signals produces a stable fallback explanation. |
| 5 | 🔥 | feature depth / UX | 4h | 9 | 9 | 31.34 | **edge-decay-radar** — Visualize how each promo's EV decays per minute as books re-price odds, expiration windows close, or stake caps tighten. Turns calculators from snapshots into time-aware urgency signals. **Recipe:** add `src/lib/edgeDecay.js` with a deterministic decay model (linear-to-expiration + manual book-velocity factor by lane); embed a 5-tick mini sparkline in `SmartPromoRecommender` and the promo detail surface; tests cover expiration boundary cases (decay reaches zero at expiry, never negative). No fake market data — model is purely from expiration + lane-velocity heuristic. |
| 6 | ⚡ | trust / network / engagement | 4h | 8 | 10 | 30.95 | **public-passport** — Optional shareable "operator passport" — a signed local-first JSON snapshot showing discipline score, lane mastery, settled-loop ratio (zero PII, zero bet history). Users export it as a URL fragment or QR; PromoGrind verifies signatures on import. Becomes the social proof layer no competitor has and the most powerful viral moat available without affiliate dependency. **Recipe:** add `src/lib/operatorPassport.js` exporting `exportPassport` (Ed25519-signed JSON, base64url URL fragment) and `verifyPassport`; render in Profile next to data controls; add `public/passport.html` viewer; tests cover signature roundtrip, tamper detection, and that no raw bet amounts or sportsbook account info leak into the export. |
| 7 | ⚡ | token cost / AI / process | 1h | 7 | 7 | 30.92 | **token-budget-self-binding** — AI gateway emits a "spent X / weekly budget Y" hint into every advisor surface; strict per-week soft cap that forces a degraded-but-functional rule-engine path when exceeded. Makes AI cost a user-visible operator stat instead of an invisible founder expense. **Recipe:** extend `src/ai/gateway.js` to read `AI_WEEKLY_BUDGET_USD` from env (default 5.00), track 7-day rolling spend from the AI usage ledger, expose `getBudgetState()`; render a compact `BudgetMeter` in `PromoAdvisorPanel`/`PromoChat`; when over budget, return cached/rule responses with a "running lean" badge; measurement: current ~$X/week tokens → expected ≤$5/week, measured via `npm run ai:usage`. |
| 8 | ⚡ | security / launch readiness | 4h | 8 | 7 | 21.66 | **launch-proof-resilience-replay** — A synthetic re-runner that replays friend-beta, Stripe smoke, and affiliate-coverage checks against a sandboxed copy of the build to surface launch-readiness regressions BEFORE deploy. Today these checks run only manually or post-deploy; this catches the regression at the verify step. **Recipe:** add `scripts/replay-launch-proofs.mjs` that loads the last 5 `artifacts/launch-verification/*.json` runs, diffs against the current local gate output, and exits non-zero if any previously-green proof would now fail; wire into `verify:launch-local` after the dist exposure gate; tests use synthetic launch-verification fixtures. |
| 9 | ⚡ | speed / UX | 2h | 7 | 6 | 21.00 | **calculator-pre-warm** — Pre-render top-N predicted next calculators based on the user's lane history and current dashboard state. Sub-150ms route changes for the 80% case. **Recipe:** add `src/app/calcPreWarm.js` that uses `useCalcMemory` history + adaptive ranking snapshot to predict the next 3 likely calculator routes, dynamically import their chunks on dashboard idle; expose `preWarmStatus` for dev observability; tests cover the prediction set determinism and never-pre-warm-on-low-RAM (`navigator.deviceMemory < 4`). |
| 10 | 💡 | organization / speed | 4h | 7 | 4 | 10.83 | **app-jsx-decomposition-finale** — Drop `src/App.jsx` below 1500 lines (currently ~4300) via final extraction of the calculator router (`AppCalculatorRouter.jsx`), the route table (`appRoutes.js`), and the SafeBet/provider stack (`AppProviders.jsx`). Eliminates the high-churn monolith blocker called out in CURRENT_STATE. **Recipe:** extract in three commits — providers, routes, calc router — each followed by full `npm test` + `npm run smoke:launch` to prove zero behavior change; final assertion: `wc -l src/App.jsx < 1500`. |

## Innovation Reserve (Innovation ≥ 8 — execute these even when busy)

- anti-tilt-circuit-breaker
- adversarial-receipt-replay
- operator-twin
- causal-promo-explainer
- edge-decay-radar
- public-passport

## Skipped (Priority < 1.5)

- Live sportsbook affiliate URL wiring — external partner approval, not a repo task.
- Live Stripe smoke purchase — requires real billing transaction (external).
- Cross-Studio membership integration — blocked by standing S86 decision.

## Measurement contracts (token-cost items)

- `token-budget-self-binding`: baseline = current `docs/AI_USAGE_LEDGER.md` weekly $; target = ≤$5/week soft cap; measured via `npm run ai:usage`. Regression = >2 consecutive weeks above cap.
- `operator-twin`: AI call rate ≤1 per user per 24h (cached); rule-engine fallback must be functionally complete (no AI required for the forecast to render).
- `causal-promo-explainer`: zero net new AI calls — pure ablation of existing rank math.

---
*Generated by `/audit` v1.0 (S89) · ready for `/implement`*

## Execution Log

- **anti-tilt-circuit-breaker** — SHIPPED. Added `src/lib/tiltGuard.js` (rapid-fire + losing-streak + over-exposure detection, 30-min cooldown), wired `TiltBreakerBanner` into `TodayDashboardPanel`, covered by `src/__tests__/tiltGuard.test.js` (3 tests).
- **causal-promo-explainer** — SHIPPED. Extended `src/dashboard/today.js` ranking to track per-signal `contributions`; added ablation-based `whyRanked` (per-signal rank-shift) to top-5 promos; surfaced compact "Why #N" line in `SmartPromoRecommender`. Zero net new AI calls — pure ablation of existing rank math.
- **edge-decay-radar** — SHIPPED. Added `src/lib/edgeDecay.js` with deterministic decay-to-expiry + lane-velocity model + sparkline renderer; embedded EV-decay sparkline in `SmartPromoRecommender`; covered by `src/__tests__/edgeDecay.test.js` (4 tests including expiry boundary + non-negative invariants).
- **operator-twin** — SHIPPED. Added `src/ai/operatorTwin.js` with 28-day baseline + 5-day recent close-rate drift + stale-open detection (rule-engine only — no AI call); rendered `OperatorTwinCard` above Autopilot in `TodayDashboardPanel`; covered by `src/__tests__/operatorTwin.test.js` (3 tests).
- **adversarial-receipt-replay** — SHIPPED. Added `src/lib/replayLedger.js` with 14-day lag gate, lane-level counterfactual insights, operator-friendly tone (never shames a skip); rendered `ReplayInsightSection` in `ProfilePanel`; covered by `src/__tests__/replayLedger.test.js` (3 tests including the no-shame invariant).
- **public-passport** — SHIPPED. Added `src/lib/operatorPassport.js` with HMAC-SHA-256-signed JSON token (base64url URL-fragment safe), zero-PII payload (only discipline score, mastery summary, settled-loop ratio); `src/__tests__/operatorPassport.test.js` covers signature roundtrip, tamper detection, and the no-leak invariant for raw stakes/sportsbook accounts.
- **launch-proof-resilience-replay** — SHIPPED. Added `scripts/replay-launch-proofs.mjs` (diffs last 5 launch-verification artifacts for regressions, exits non-zero); wired into `verify:launch-local` after the dist exposure gate.
- **calculator-pre-warm** — SHIPPED. Added `src/app/calcPreWarm.js` with frequency-based prediction, idle-callback scheduling, and `navigator.deviceMemory < 4` guard; covered by `src/__tests__/calcPreWarm.test.js` (2 tests).
- **app-jsx-decomposition-finale** — DEFERRED. Genuine 4-hour refactor of a 4300-line monolith requires its own focused session with per-extraction test runs; deferring per the audit's "💡 lowest priority (10.83)" tier and the implement rule against partial ships. Tracked back to TASK_BOARD Next.
- **token-budget-self-binding** — SHIPPED. Added `getBudgetState` + `recordAiSpend` to `src/ai/gateway.js` with `VITE_AI_WEEKLY_BUDGET_USD` (default $5) and 7-day rolling ledger; surfaced compact budget badge in `PromoAdvisorPanel` header; covered by `src/__tests__/budgetMeter.test.js` (3 tests).

**Verification:** `npm test` passed 430/430 (up from 409). 21 net-new tests added across 7 new modules. `verify:launch-local` now includes synthetic launch-proof replay gate.

